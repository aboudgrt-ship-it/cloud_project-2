"""
═══════════════════════════════════════════════════════════════════════════════
                        دليل AWS IAM الشامل
                     AWS IAM Complete Guide - Arabic
═══════════════════════════════════════════════════════════════════════════════

IAM = Identity and Access Management
الإدارة: من يمكنه الوصول؟ وماذا يمكنه أن يفعل؟

هذا الدليل يشرح IAM من الصفر إلى الاحتراف بالعربية
"""

print("""
╔════════════════════════════════════════════════════════════════════════════╗
║                                                                            ║
║                      🔐 دليل AWS IAM الشامل                              ║
║                                                                            ║
║              ما هو IAM؟ كيف نستخدمه؟ أمثلة عملية                        ║
║                                                                            ║
╚════════════════════════════════════════════════════════════════════════════╝


════════════════════════════════════════════════════════════════════════════════
الجزء 1: ما هو IAM؟
════════════════════════════════════════════════════════════════════════════════

IAM = Identity and Access Management

التعريف البسيط:
───────────────
IAM هي خدمة AWS تتحكم في من يمكنه الوصول إلى خدمات AWS وماذا يمكنه أن يفعل.

تشبيه:
────────
تخيل شركة بها:
- مدير (Admin)         → يمكنه فعل أي شيء
- موظف مبيعات         → يمكنه فقط قراءة البيانات
- محاسب               → يمكنه فقط التعامل مع الفواتير
- حراس أمن            → يمكنهم فقط الدخول والخروج

IAM يعمل بنفس الطريقة!


════════════════════════════════════════════════════════════════════════════════
الجزء 2: المفاهيم الأساسية
════════════════════════════════════════════════════════════════════════════════

1. User (المستخدم)
   ─────────────────
   ماذا: شخص أو تطبيق
   مثال: ahmed@company.com
   
   خصائص:
   - اسم المستخدم (Username)
   - كلمة المرور (Password)
   - مفاتيح الوصول (Access Keys)
   - الأذونات (Permissions)
   
   مثال في AWS:
   ┌─────────────────────────────────────┐
   │ User: "database-admin"              │
   │ Permissions:                        │
   │ ├─ s3:GetObject                     │
   │ ├─ s3:PutObject                     │
   │ └─ rds:DescribeDBInstances          │
   └─────────────────────────────────────┘


2. Group (المجموعة)
   ────────────────
   ماذا: مجموعة من المستخدمين
   مثال: فريق المبيعات
   
   الفائدة: إدارة الأذونات لعدة مستخدمين معاً
   
   مثال في AWS:
   ┌─────────────────────────────────────┐
   │ Group: "Sales Team"                 │
   │ Members:                            │
   │ ├─ user1                            │
   │ ├─ user2                            │
   │ └─ user3                            │
   │ Permissions: s3:ListBucket          │
   └─────────────────────────────────────┘


3. Role (الدور)
   ────────────
   ماذا: مجموعة من الأذونات بدون هوية معينة
   مثال: دور لـ EC2 instance
   
   الفرق:
   - User = شخص محدد
   - Role = مجرد أذونات (بدون هوية)
   
   الاستخدام:
   - EC2 instance يحتاج الوصول إلى S3
   - Lambda function تحتاج الوصول إلى DynamoDB
   - Service يحتاج الوصول لخدمة أخرى
   
   مثال في AWS:
   ┌─────────────────────────────────────┐
   │ Role: "EC2-S3-Access"               │
   │ Used by: EC2 instances              │
   │ Permissions:                        │
   │ ├─ s3:GetObject                     │
   │ ├─ s3:PutObject                     │
   │ └─ s3:ListBucket                    │
   └─────────────────────────────────────┘


4. Policy (السياسة)
   ────────────────
   ماذا: مستند JSON يحدد الأذونات
   
   البنية الأساسية:
   {
       "Version": "2012-10-17",
       "Statement": [
           {
               "Effect": "Allow",          // السماح
               "Action": "s3:*",           // أي عملية؟
               "Resource": "*"             // على أي موارد؟
           }
       ]
   }
   
   مثال: سياسة تسمح بقراءة S3 فقط
   {
       "Version": "2012-10-17",
       "Statement": [
           {
               "Effect": "Allow",
               "Action": ["s3:GetObject", "s3:ListBucket"],
               "Resource": "arn:aws:s3:::my-bucket/*"
           }
       ]
   }


════════════════════════════════════════════════════════════════════════════════
الجزء 3: العلاقات بين المفاهيم
════════════════════════════════════════════════════════════════════════════════

User
  ↓
(ينتمي إلى)
  ↓
Group ← (لها) ← Policy
  ↓
(منح)
  ↓
Permissions (الأذونات)


أو:

Role ← (لها) ← Policy
  ↓
(تُعطى إلى)
  ↓
EC2 Instance / Lambda / User


مثال عملي:
───────────
الحالة: موظف مبيعات يحتاج الوصول إلى S3 فقط

1. إنشاء Policy
   {
       "Effect": "Allow",
       "Action": ["s3:GetObject", "s3:ListBucket"],
       "Resource": "*"
   }

2. إنشاء Group "Sales Team"
   ├─ إضافة Policy إليه

3. إنشاء User "salesman1"
   ├─ إضافته إلى Group "Sales Team"

4. النتيجة:
   salesman1 → عضو في Sales Team → له نفس أذونات المجموعة


════════════════════════════════════════════════════════════════════════════════
الجزء 4: أنواع الوصول
════════════════════════════════════════════════════════════════════════════════

1. Console Access (وصول الويب)
   ──────────────────────────
   - اسم المستخدم + كلمة المرور
   - تسجيل الدخول من https://console.aws.amazon.com
   
   مثال:
   username: ahmed
   password: SecurePassword123


2. Programmatic Access (وصول برمجي)
   ──────────────────────────────────
   - Access Key ID
   - Secret Access Key
   - مفاتيح لاستخدام boto3 و AWS CLI
   
   مثال:
   Access Key ID:     AKIA2XXXXXXXXX
   Secret Key:        wJalrXUtnFEMI/K7MDENG/xxxxx


════════════════════════════════════════════════════════════════════════════════
الجزء 5: الأذونات الأساسية
════════════════════════════════════════════════════════════════════════════════

Effect: السماح أو الرفض
────────────────────
- Allow    = مسموح
- Deny     = مرفوع


Action: نوع العملية
──────────────────
S3:
- s3:GetObject          قراءة ملف
- s3:PutObject          رفع ملف
- s3:DeleteObject       حذف ملف
- s3:ListBucket         عرض قائمة الملفات

IAM:
- iam:CreateUser        إنشاء مستخدم
- iam:DeleteUser        حذف مستخدم
- iam:GetUser           عرض معلومات

EC2:
- ec2:RunInstances      تشغيل instance
- ec2:StopInstances     إيقاف instance
- ec2:TerminateInstances حذف instance

Resource: على أي موارد؟
─────────────────────
- "*"                   كل الموارد
- "arn:aws:s3:::mybucket/*"   ملفات محددة
- "arn:aws:iam::account:user/john"  مستخدم محدد


════════════════════════════════════════════════════════════════════════════════
الجزء 6: مثال كامل - إنشاء مستخدم مع أذونات
════════════════════════════════════════════════════════════════════════════════

الحالة: تطبيق يحتاج الوصول إلى S3 فقط

الخطوة 1: إنشاء Policy
───────────────────────
Policy Name: "S3-ReadWrite"
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Action": [
                "s3:GetObject",
                "s3:PutObject",
                "s3:DeleteObject",
                "s3:ListBucket"
            ],
            "Resource": [
                "arn:aws:s3:::my-app-bucket",
                "arn:aws:s3:::my-app-bucket/*"
            ]
        }
    ]
}


الخطوة 2: إنشاء User
───────────────────
Username: "app-user"
Access Type: Programmatic access


الخطوة 3: إضافة Policy للـ User
────────────────────────────────
اربط Policy "S3-ReadWrite" مع User "app-user"


الخطوة 4: الحصول على المفاتيح
──────────────────────────────
Access Key ID:     AKIA5XXXXXXXXXXX
Secret Access Key: wJalrXUtnFEMI/K7XXXXXXXXXXXXX

(احفظها في مكان آمن!)


الخطوة 5: الاستخدام في البرنامج
───────────────────────────────
import boto3

s3 = boto3.client(
    's3',
    aws_access_key_id='AKIA5XXXXXXXXXXX',
    aws_secret_access_key='wJalrXUtnFEMI/K7XXXXXXXXXXXXX'
)

# الآن app-user يمكنه فقط:
# ✓ قراءة ملفات من S3
# ✓ رفع ملفات إلى S3
# ✓ حذف ملفات من S3
# ✗ لا يمكنه الوصول إلى EC2، DynamoDB، إلخ


════════════════════════════════════════════════════════════════════════════════
الجزء 7: أفضل الممارسات
════════════════════════════════════════════════════════════════════════════════

❌ خطأ شائع 1: استخدام Root Account
───────────────────────────────────
# لا تفعل هذا!
aws_access_key_id = 'AKIAIOSFODNN7EXAMPLE'  # ❌ مفتاح Root!

✅ الحل:
# أنشئ IAM user بأذونات محدودة
user = iam.create_user(UserName='app-user')


❌ خطأ شائع 2: أذونات عامة جداً
──────────────────────────────
{
    "Effect": "Allow",
    "Action": "*",              # ❌ كل شيء!
    "Resource": "*"             # ❌ كل الموارد!
}

✅ الحل: استخدم principle of least privilege
{
    "Effect": "Allow",
    "Action": ["s3:GetObject"],  # ✓ فقط قراءة
    "Resource": "arn:aws:s3:::my-bucket/*"  # ✓ bucket محدد
}


❌ خطأ شائع 3: حفظ المفاتيح في الكود
────────────────────────────────
# لا تفعل هذا!
AWS_KEY = 'AKIA5XXXXXXXXXXX'  # ❌ في الكود!

✅ الحل: استخدم متغيرات البيئة أو AWS Secrets Manager
import os
AWS_KEY = os.getenv('AWS_ACCESS_KEY_ID')  # ✓ من البيئة


❌ خطأ شائع 4: عدم استخدام MFA
─────────────────────────────
# حساب بدون حماية إضافية ❌

✅ الحل: فعّل Multi-Factor Authentication (MFA)
على Root Account و أي حساب حساس


❌ خطأ شائع 5: عدم تحديث المفاتيح
────────────────────────────────
# استخدام نفس المفتاح لسنوات ❌

✅ الحل: غيّر المفاتيح كل 3 أشهر
1. أنشئ مفتاح جديد
2. استخدمه في التطبيق
3. احذف المفتاح القديم


════════════════════════════════════════════════════════════════════════════════
الجزء 8: خطوات البدء الفعلية
════════════════════════════════════════════════════════════════════════════════

الخطوة 1: الدخول إلى AWS Console
─────────────────────────────────
1. اذهب إلى https://console.aws.amazon.com
2. سجّل الدخول بحساب Root
3. ابحث عن "IAM" في الخدمات


الخطوة 2: فهم الـ Dashboard
───────────────────────────
في IAM Dashboard ستجد:
├─ Users          (المستخدمون)
├─ Groups         (المجموعات)
├─ Roles          (الأدوار)
├─ Policies       (السياسات)
├─ Access Analyzer (تحليل الوصول)
└─ Account Security (أمان الحساب)


الخطوة 3: إنشاء أول User
─────────────────────────
1. اذهب إلى Users
2. اضغط "Create user"
3. ادخل اسم المستخدم
4. اختر "Programmatic access"
5. ضع أذونات (Policy)
6. احصل على Access Key و Secret Key


الخطوة 4: فهم الـ ARN
────────────────────
ARN = Amazon Resource Name (اسم موارد AWS)

الصيغة:
arn:partition:service:region:account-id:resource-type/resource-id

مثال S3:
arn:aws:s3:::my-bucket/*

مثال IAM:
arn:aws:iam::123456789012:user/john

مثال EC2:
arn:aws:ec2:us-east-1:123456789012:instance/i-1234567890abcdef0


════════════════════════════════════════════════════════════════════════════════
الجزء 9: الفرق بين الأدوار (Roles) والمستخدمين (Users)
════════════════════════════════════════════════════════════════════════════════

User:
└─ هوية محددة
└─ شخص أو تطبيق
└─ له بيانات اعتماد (username/password أو access keys)
└─ مثال: مبرمج في الشركة


Role:
└─ بدون هوية محددة
└─ مجموعة أذونات فقط
└─ تُعطى إلى AWS services
└─ مثال: EC2 instance يحتاج الوصول إلى S3


الحالة العملية:
────────────────
1. EC2 Instance يحتاج الوصول إلى S3

   الطريقة الخاطئة:
   └─ حفظ Access Keys داخل EC2 ❌ خطر!
   
   الطريقة الصحيحة:
   1. أنشئ Role "EC2-S3-Access"
   2. أضف Policy للـ Role
   3. ربط الـ Role مع EC2 Instance
   └─ الـ EC2 سيحصل تلقائياً على الأذونات ✓


════════════════════════════════════════════════════════════════════════════════
الجزء 10: أنواع الـ Policies
════════════════════════════════════════════════════════════════════════════════

1. AWS Managed Policies
   ──────────────────────
   - سياسات جاهزة من AWS
   - مثال: "AmazonS3ReadOnlyAccess"
   - الفائدة: سهل وآمن
   
   أمثلة شهيرة:
   - AmazonS3ReadOnlyAccess    قراءة S3 فقط
   - AmazonEC2ReadOnlyAccess   قراءة EC2 فقط
   - PowerUserAccess           كل شيء إلا IAM


2. Customer Managed Policies
   ──────────────────────────
   - سياسات تنشئها أنت
   - كاملة التحكم
   - الفائدة: دقة عالية
   
   مثال:
   {
       "Version": "2012-10-17",
       "Statement": [...]
   }


3. Inline Policies
   ────────────────
   - سياسة مرتبطة مباشرة بـ user/group/role
   - لا توجد بشكل منفصل
   - الفائدة: سهلة للحالات البسيطة
   - العيب: صعب لإدارة عديد من الـ users


════════════════════════════════════════════════════════════════════════════════
الخلاصة: ماذا تعلمنا؟
════════════════════════════════════════════════════════════════════════════════

✓ ماهو IAM؟
   → خدمة للتحكم في من يمكنه الوصول وماذا يمكنه فعل

✓ المفاهيم الأساسية:
   → User (مستخدم)
   → Group (مجموعة)
   → Role (دور)
   → Policy (سياسة)

✓ الأذونات:
   → Effect (Allow/Deny)
   → Action (العملية)
   → Resource (الموارد)

✓ أفضل الممارسات:
   → أذونات محدودة (principle of least privilege)
   → عدم استخدام Root Account
   → عدم حفظ المفاتيح في الكود
   → تفعيل MFA
   → تحديث المفاتيح بانتظام

✓ الخطوات العملية:
   → إنشاء User
   → إضافة Policy
   → الحصول على Access Keys
   → الاستخدام مع boto3


════════════════════════════════════════════════════════════════════════════════
الخطوة التالية
════════════════════════════════════════════════════════════════════════════════

1. اقرأ IAM_DETAILED_GUIDE.py     (شرح مفصل)
2. اقرأ BOTO3_IAM_EXAMPLES.py     (أمثلة عملية)
3. جرّب إنشاء user بنفسك في AWS Console
4. استخدم boto3 لإدارة IAM برمجياً

""")

print("\n✅ انتهى الشرح الأساسي لـ IAM")
print("🚀 الملفات التالية:")
print("  1. IAM_DETAILED_GUIDE.py - شرح مفصل بالكود")
print("  2. BOTO3_IAM_EXAMPLES.py - أمثلة عملية مع boto3")
print("  3. IAM_QUICK_REFERENCE.md - مرجع سريع")
