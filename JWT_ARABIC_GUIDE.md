# شرح نظام JWT في Flask باللغة العربية

### ما هو الـ JWT؟
هو وسيلة آمنة لنقل المعلومات بين طرفين (العميل والخادم) ككائن JSON. يتم توقيع هذا الرمز رقمياً بحيث لا يمكن التلاعب به.

---

### شرح الوظائف البرمجية (Functions):

#### 1. `JWTManager(app)`
هذا هو "المدير المسئول". يقوم بربط تطبيق Flask الخاص بك بمكتبة JWT. بدونه، لن يفهم Flask كيفية التعامل مع الرموز المشفرة.

#### 2. `create_access_token(identity=...)`
هذا "مصنع الرموز". نستخدمه عندما يسجل المستخدم دخوله بنجاح.
- الـ **identity**: هي معلومة فريدة (مثل اسم المستخدم أو الـ ID) نضعها داخل الرمز لنعرف صاحب الرمز لاحقاً.

#### 3. `@jwt_required()`
هذا هو "حارس الأمن". نضعه فوق أي مسار (Route) نريد حمايته.
- إذا حاول شخص الدخول للمسار بدون رمز، سيقوم الحارس بمنعه فوراً وإرسال رسالة "غير مصرح لك".

#### 4. `get_jwt_identity()`
هذا "المحقق". داخل المسارات المحمية، نستخدمه لنعرف: "من هو المستخدم الذي أرسل هذا الرمز الآن؟". يقوم باستخراج الهوية التي وضعناها سابقاً في الخطوة رقم 2.

---

### كيف يعمل النظام بالترتيب؟
1. العميل يرسل كلمة السر.
2. الخادم يتأكد منها ويصنع رمزاً باستخدام `create_access_token`.
3. العميل يحفظ الرمز لديه.
4. العميل يرسل الرمز عند طلب أي صفحة محمية بـ `@jwt_required`.
5. الخادم يستخدم `get_jwt_identity` ليعرف من هو المستخدم ويقدم له بياناته الخاصة.
